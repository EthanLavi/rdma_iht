#include "iht_ds.h"
#include "rome/rdma/memory_pool/memory_pool.h"

using ::rome::rdma::MemoryPool;

constexpr char iphost[] = "10.10.1.1"
constexpr char ippeer[] = "10.10.1.2"

int main(){
    MemoryPool::Peer host = Peer();
}